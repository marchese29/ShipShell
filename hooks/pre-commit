#!/bin/bash
# Pre-commit hook: runs linting, formatting, and type checking

set -e

echo "Running ruff..."
uv run ruff check shell/

echo "Running ruff format..."
uv run ruff format --check shell/

echo "Running pyright..."
uv run pyright shell/

echo "All checks passed!"
