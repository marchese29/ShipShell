"""
ShipShell Ergonomics Package

This package provides ergonomic wrappers for shell builtins and POSIX utilities.

Usage in ShipShell scripts:
    from shp.ergo.builtins import cd, pwd, pushd, popd, dirs
    from shp.ergo.posix import awk, grep, sed, cat, ls
"""
